{
  "name": "vector-rocks",
  "private": true,
  "version": "0.0.0",
  "description": "Vector Rocks — a deluxe-style vector shooter (browser, Canvas + TypeScript)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^8.0.1"
  }
}
